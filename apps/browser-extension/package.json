{
  "name": "@prism/browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Prism browser extension for AI chat contextual ads",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "lint": "tsc --noEmit",
    "package": "npm run build && zip -r prism-browser-extension.zip dist/"
  },
  "dependencies": {
    "@prism/shared": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0",
    "esbuild": "^0.20",
    "typescript": "^5"
  }
}
