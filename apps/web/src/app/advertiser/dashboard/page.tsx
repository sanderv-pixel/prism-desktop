// Advertiser dashboard now renders the v2 design (see
// app/advertiser/dashboard-v2/page.tsx). The previous implementation remains in
// git history on `main`.
export { default } from '../dashboard-v2/page'
